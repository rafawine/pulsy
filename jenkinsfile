pipeline {
    agent any

    tools {
        go '1.23.6'
    }

    stages {
        stage("build") {
            steps{
                sh './jenkins/build.sh'
            }
        }
        stage("deploy") {
            steps{
                sh './jenkins/deploy.sh'
            }
        }
    }
}